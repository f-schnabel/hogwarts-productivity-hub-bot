<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="dark only">
  <title>{{ title }} - Hogwarts Productivity</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;500;600;700&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
  {% if includeChartJs %}
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  {% endif %}
  <style>
    :root {
      color-scheme: dark only;
      --bg-deep: #0a0a12;
      --bg-surface: #12121c;
      --bg-card: rgba(20, 20, 35, 0.7);
      --bg-card-hover: rgba(30, 30, 50, 0.8);
      --text-primary: #f0e6d3;
      --text-secondary: rgba(240, 230, 211, 0.7);
      --text-muted: rgba(240, 230, 211, 0.5);
      --accent-gold: #d4a853;
      --accent-gold-bright: #f0c674;
      --border-subtle: rgba(212, 168, 83, 0.15);
      --border-glow: rgba(212, 168, 83, 0.3);
      --shadow-ambient: 0 8px 32px rgba(0, 0, 0, 0.4);
      --shadow-glow: 0 0 40px rgba(212, 168, 83, 0.1);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Outfit', system-ui, sans-serif;
      font-weight: 400;
      background: var(--bg-deep);
      color: var(--text-primary);
      min-height: 100vh;
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* Atmospheric background */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background:
        radial-gradient(ellipse 80% 50% at 50% -20%, rgba(212, 168, 83, 0.08) 0%, transparent 50%),
        radial-gradient(ellipse 60% 40% at 80% 100%, rgba(100, 60, 120, 0.1) 0%, transparent 50%),
        radial-gradient(ellipse 50% 30% at 10% 80%, rgba(60, 80, 120, 0.08) 0%, transparent 50%);
      pointer-events: none;
      z-index: 0;
    }

    /* Noise texture overlay */
    body::after {
      content: '';
      position: fixed;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
      opacity: 0.03;
      pointer-events: none;
      z-index: 1;
    }

    .page-wrapper {
      position: relative;
      z-index: 2;
      padding: 2rem;
      min-height: 100vh;
    }

    .container {
      max-width: 960px;
      margin: 0 auto;
    }

    /* Navigation */
    nav {
      display: flex;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: 3rem;
      padding: 1rem;
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      border-radius: 16px;
      animation: fadeSlideDown 0.6s ease-out;
    }

    nav a {
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 0.95rem;
      font-weight: 500;
      letter-spacing: 0.02em;
      padding: 0.6rem 1.4rem;
      border-radius: 10px;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
    }

    nav a:hover {
      color: var(--accent-gold);
      background: rgba(212, 168, 83, 0.1);
    }

    nav a::after {
      content: '';
      position: absolute;
      bottom: 0.4rem;
      left: 50%;
      width: 0;
      height: 2px;
      background: var(--accent-gold);
      transition: all 0.3s ease;
      transform: translateX(-50%);
      border-radius: 1px;
    }

    nav a:hover::after {
      width: 60%;
    }

    /* Typography */
    h1, h2, h3 {
      font-family: 'Cinzel', serif;
      font-weight: 600;
      letter-spacing: 0.04em;
    }

    h1 {
      font-size: clamp(1.8rem, 5vw, 2.8rem);
      text-align: center;
      margin-bottom: 2.5rem;
      background: linear-gradient(135deg, var(--text-primary) 0%, var(--accent-gold) 50%, var(--text-primary) 100%);
      background-size: 200% auto;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: shimmerText 8s linear infinite, fadeSlideDown 0.8s ease-out 0.1s both;
    }

    h2 {
      font-size: 1.1rem;
      color: var(--text-secondary);
      margin-bottom: 1rem;
    }

    /* Cards - Glass morphism */
    .card {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      border-radius: 20px;
      padding: 1.75rem;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(255,255,255,0.03) 0%, transparent 50%);
      pointer-events: none;
    }

    .card:hover {
      background: var(--bg-card-hover);
      border-color: var(--border-glow);
      transform: translateY(-4px);
      box-shadow: var(--shadow-ambient), var(--shadow-glow);
    }

    /* House Grid */
    .house-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.25rem;
      margin: 2rem 0;
    }

    .house-card {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 2rem 1.5rem;
      text-align: center;
      border: 1px solid var(--border-subtle);
      position: relative;
      overflow: hidden;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .house-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: var(--house-color);
      opacity: 0.8;
    }

    .house-card::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 80px;
      background: linear-gradient(180deg, var(--house-color-fade) 0%, transparent 100%);
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .house-card:hover {
      transform: translateY(-6px) scale(1.02);
      border-color: var(--house-color);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3), 0 0 30px var(--house-color-fade);
    }

    .house-card:hover::after {
      opacity: 1;
    }

    .house-card h2 {
      font-family: 'Cinzel', serif;
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
      position: relative;
      z-index: 1;
    }

    .house-card .points {
      font-size: 2.5rem;
      font-weight: 600;
      font-family: 'Outfit', sans-serif;
      letter-spacing: -0.02em;
      position: relative;
      z-index: 1;
    }

    .house-card .members {
      font-size: 0.85rem;
      color: var(--text-muted);
      margin-top: 0.75rem;
      position: relative;
      z-index: 1;
    }

    /* Table Styles */
    .table-wrapper {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      border-radius: 20px;
      overflow: hidden;
      animation: fadeSlideUp 0.6s ease-out 0.2s both;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th {
      font-family: 'Cinzel', serif;
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--text-muted);
      text-align: left;
      padding: 1.25rem 1.5rem;
      background: rgba(0, 0, 0, 0.3);
      border-bottom: 1px solid var(--border-subtle);
    }

    td {
      padding: 1rem 1.5rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.03);
      transition: background 0.2s ease;
    }

    tr:last-child td {
      border-bottom: none;
    }

    tbody tr {
      transition: all 0.2s ease;
    }

    tbody tr:hover {
      background: rgba(212, 168, 83, 0.05);
    }

    tbody tr:hover td {
      border-bottom-color: transparent;
    }

    .rank {
      font-family: 'Cinzel', serif;
      font-weight: 700;
      color: var(--accent-gold);
      font-size: 0.95rem;
    }

    .rank-1 { color: #ffd700; text-shadow: 0 0 10px rgba(255, 215, 0, 0.5); }
    .rank-2 { color: #c0c0c0; text-shadow: 0 0 10px rgba(192, 192, 192, 0.3); }
    .rank-3 { color: #cd7f32; text-shadow: 0 0 10px rgba(205, 127, 50, 0.3); }

    .user-link {
      color: var(--text-primary);
      text-decoration: none;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    .user-link:hover {
      color: var(--accent-gold);
    }

    /* User Profile */
    .user-header {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      padding: 3rem 2rem;
      border-radius: 24px;
      text-align: center;
      margin-bottom: 2rem;
      position: relative;
      overflow: hidden;
      animation: fadeSlideDown 0.6s ease-out;
    }

    .user-header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, transparent, var(--house-color), transparent);
    }

    .user-header h1 {
      margin-bottom: 0.5rem;
    }

    .user-house {
      font-family: 'Cinzel', serif;
      font-size: 1rem;
      font-weight: 500;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    /* Stats Grid */
    .stat-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: 1rem;
      margin: 1.5rem 0;
    }

    .stat {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      padding: 1.5rem 1rem;
      border-radius: 16px;
      text-align: center;
      transition: all 0.3s ease;
    }

    .stat:hover {
      border-color: var(--border-glow);
      transform: translateY(-2px);
    }

    .stat-value {
      font-size: 1.75rem;
      font-weight: 600;
      color: var(--accent-gold-bright);
      letter-spacing: -0.02em;
    }

    .stat-label {
      font-size: 0.75rem;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin-top: 0.5rem;
    }

    /* Year Progress */
    .year-rank {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      border-radius: 20px;
      padding: 2rem;
      margin: 2rem 0;
      animation: fadeSlideUp 0.6s ease-out 0.3s both;
    }

    .year-badge {
      font-family: 'Cinzel', serif;
      font-size: 2rem;
      font-weight: 700;
      letter-spacing: 0.05em;
    }

    .progress-container {
      background: rgba(0, 0, 0, 0.4);
      border-radius: 14px;
      height: 32px;
      overflow: hidden;
      position: relative;
      margin: 1.25rem 0;
      border: 1px solid rgba(255, 255, 255, 0.05);
    }

    .progress-bar {
      height: 100%;
      border-radius: 14px;
      background: linear-gradient(90deg, var(--bar-start) 0%, var(--bar-end) 100%);
      box-shadow: 0 0 15px var(--bar-glow);
      position: relative;
      transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .progress-bar::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      box-shadow: 0 0 30px var(--bar-glow), 0 0 50px var(--bar-glow);
      opacity: 0;
      animation: barGlowPulse 5s ease-in-out infinite;
    }

    @keyframes barGlowPulse {
      0%, 100% { opacity: 0; }
      50% { opacity: 1; }
    }

    .progress-info {
      display: flex;
      align-items: center;
      font-size: 0.85rem;
      color: var(--text-muted);
      margin-top: 0.75rem;
    }

    .progress-info > span:first-child {
      flex: 1;
      text-align: left;
    }

    .progress-info > span:last-child {
      flex: 1;
      text-align: right;
    }

    .progress-text {
      font-weight: 600;
      font-size: 0.9rem;
      letter-spacing: 0.02em;
      color: rgba(240, 230, 211, 0.8);
      text-align: center;
    }

    .next-rank {
      color: var(--accent-gold);
    }

    /* Sessions / Chart area */
    .sessions {
      margin-top: 2rem;
      animation: fadeSlideUp 0.6s ease-out 0.4s both;
    }

    .chart-container {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      border-radius: 20px;
      padding: 1.5rem;
    }

    /* Footer text */
    .footer-note {
      text-align: center;
      color: var(--text-muted);
      font-size: 0.85rem;
      margin-top: 3rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border-subtle);
    }

    /* Leaderboard Controls */
    .leaderboard-controls {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1.5rem;
      animation: fadeSlideDown 0.5s ease-out 0.15s both;
    }

    .house-filters {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    .filter-pill {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      border-radius: 20px;
      padding: 0.5rem 1rem;
      font-size: 0.85rem;
      font-weight: 500;
      color: var(--text-secondary);
      cursor: pointer;
      transition: all 0.3s ease;
      font-family: inherit;
    }

    .filter-pill:hover {
      border-color: var(--border-glow);
      color: var(--text-primary);
    }

    .filter-pill.active {
      background: rgba(212, 168, 83, 0.15);
      border-color: var(--accent-gold);
      color: var(--accent-gold);
      box-shadow: 0 0 12px rgba(212, 168, 83, 0.2);
    }

    .filter-pill.gryffindor { --pill-color: #ff2000; }
    .filter-pill.hufflepuff { --pill-color: #f8d301; }
    .filter-pill.ravenclaw { --pill-color: #5b7fc7; }
    .filter-pill.slytherin { --pill-color: #07ad34; }

    .filter-pill.gryffindor:hover,
    .filter-pill.gryffindor.active { border-color: var(--pill-color); color: var(--pill-color); background: rgba(255, 32, 0, 0.1); box-shadow: 0 0 12px rgba(255, 32, 0, 0.2); }
    .filter-pill.hufflepuff:hover,
    .filter-pill.hufflepuff.active { border-color: var(--pill-color); color: var(--pill-color); background: rgba(248, 211, 1, 0.1); box-shadow: 0 0 12px rgba(248, 211, 1, 0.2); }
    .filter-pill.ravenclaw:hover,
    .filter-pill.ravenclaw.active { border-color: var(--pill-color); color: var(--pill-color); background: rgba(91, 127, 199, 0.1); box-shadow: 0 0 12px rgba(91, 127, 199, 0.2); }
    .filter-pill.slytherin:hover,
    .filter-pill.slytherin.active { border-color: var(--pill-color); color: var(--pill-color); background: rgba(7, 173, 52, 0.1); box-shadow: 0 0 12px rgba(7, 173, 52, 0.2); }

    .sort-controls {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .sort-label {
      font-size: 0.85rem;
      color: var(--text-muted);
      margin-right: 0.25rem;
    }

    .sort-btn {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-subtle);
      border-radius: 10px;
      padding: 0.4rem 0.8rem;
      font-size: 0.8rem;
      font-weight: 500;
      color: var(--text-secondary);
      cursor: pointer;
      transition: all 0.3s ease;
      font-family: inherit;
    }

    .sort-btn:hover {
      border-color: var(--border-glow);
      color: var(--text-primary);
    }

    .sort-btn.active {
      background: rgba(212, 168, 83, 0.1);
      border-color: var(--accent-gold);
      color: var(--accent-gold);
    }

    /* Sortable column headers */
    .sortable-col {
      cursor: pointer;
      user-select: none;
    }

    .sortable-col:hover {
      color: var(--accent-gold);
    }

    .sort-arrow {
      display: inline-block;
      width: 0;
      height: 0;
      margin-left: 4px;
      vertical-align: middle;
      opacity: 0.3;
      transition: opacity 0.2s ease, transform 0.2s ease;
    }

    .sort-arrow.active {
      opacity: 1;
    }

    .sort-arrow.desc::after {
      content: '';
      display: inline-block;
      border-left: 4px solid transparent;
      border-right: 4px solid transparent;
      border-top: 5px solid currentColor;
    }

    .sort-arrow.asc::after {
      content: '';
      display: inline-block;
      border-left: 4px solid transparent;
      border-right: 4px solid transparent;
      border-bottom: 5px solid currentColor;
    }

    /* Year badges */
    .year-cell {
      white-space: nowrap;
    }

    .year-badge {
      display: inline-block;
      padding: 0.2rem 0.6rem;
      border-radius: 8px;
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.02em;
    }

    .year-badge.year-0 { background: rgba(100, 100, 100, 0.3); color: #888; }
    .year-badge.year-1 { background: rgba(139, 69, 19, 0.3); color: #cd8b62; }
    .year-badge.year-2 { background: rgba(205, 127, 50, 0.3); color: #daa520; }
    .year-badge.year-3 { background: rgba(192, 192, 192, 0.3); color: #d3d3d3; }
    .year-badge.year-4 { background: rgba(255, 215, 0, 0.3); color: #ffd700; }
    .year-badge.year-5 { background: rgba(0, 206, 209, 0.3); color: #40e0d0; }
    .year-badge.year-6 { background: rgba(147, 112, 219, 0.3); color: #ba55d3; }
    .year-badge.year-7 { background: rgba(255, 215, 0, 0.4); color: #ffd700; box-shadow: 0 0 8px rgba(255, 215, 0, 0.3); }

    /* Expandable points */
    .points-cell {
      position: relative;
    }

    .points-main {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .points-total {
      font-weight: 600;
      color: var(--accent-gold-bright);
    }

    .expand-btn {
      background: none;
      border: none;
      cursor: pointer;
      padding: 0.2rem;
      color: var(--text-muted);
      transition: all 0.2s ease;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .expand-btn:hover {
      color: var(--accent-gold);
    }

    .points-cell.expanded .expand-btn {
      transform: rotate(180deg);
      color: var(--accent-gold);
    }

    .points-breakdown {
      display: none;
      flex-direction: column;
      gap: 0.25rem;
      margin-top: 0.5rem;
      padding: 0.5rem;
      background: rgba(0, 0, 0, 0.2);
      border-radius: 8px;
      font-size: 0.8rem;
    }

    .points-cell.expanded .points-breakdown {
      display: flex;
      animation: slideDown 0.2s ease-out;
    }

    .breakdown-item {
      color: var(--text-secondary);
    }

    .breakdown-item.voice { color: #5b9bd5; }
    .breakdown-item.todo { color: #70c97f; }

    @keyframes slideDown {
      from { opacity: 0; transform: translateY(-8px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Animations */
    @keyframes fadeSlideDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeSlideUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes shimmerText {
      0% { background-position: 200% center; }
      100% { background-position: -200% center; }
    }

    /* Staggered animations for grid items */
    .house-card:nth-child(1) { animation: fadeSlideUp 0.5s ease-out 0.1s both; }
    .house-card:nth-child(2) { animation: fadeSlideUp 0.5s ease-out 0.2s both; }
    .house-card:nth-child(3) { animation: fadeSlideUp 0.5s ease-out 0.3s both; }
    .house-card:nth-child(4) { animation: fadeSlideUp 0.5s ease-out 0.4s both; }

    .stat:nth-child(1) { animation: fadeSlideUp 0.4s ease-out 0.1s both; }
    .stat:nth-child(2) { animation: fadeSlideUp 0.4s ease-out 0.15s both; }
    .stat:nth-child(3) { animation: fadeSlideUp 0.4s ease-out 0.2s both; }
    .stat:nth-child(4) { animation: fadeSlideUp 0.4s ease-out 0.25s both; }
    .stat:nth-child(5) { animation: fadeSlideUp 0.4s ease-out 0.3s both; }

    /* Responsive */
    @media (max-width: 640px) {
      .page-wrapper { padding: 1rem; }
      nav { flex-wrap: wrap; }
      th, td { padding: 0.75rem 1rem; font-size: 0.9rem; }
      .house-card { padding: 1.5rem 1rem; }
      .user-header { padding: 2rem 1.5rem; }

      .leaderboard-controls {
        flex-direction: column;
        align-items: flex-start;
      }

      .house-filters {
        width: 100%;
        justify-content: flex-start;
      }

      .filter-pill {
        padding: 0.4rem 0.7rem;
        font-size: 0.75rem;
      }

      .sort-controls {
        width: 100%;
        justify-content: flex-start;
      }

      .year-badge {
        font-size: 0.65rem;
        padding: 0.15rem 0.4rem;
      }

      .points-breakdown {
        font-size: 0.7rem;
      }
    }
  </style>
</head>
<body data-theme="dark">
  <div class="page-wrapper">
    <div class="container">
      <nav>
        <a href="/">Houses</a>
        <a href="/leaderboard">Leaderboard</a>
      </nav>
      {% block content %}{% endblock %}
    </div>
  </div>
  {% block scripts %}{% endblock %}
</body>
</html>
